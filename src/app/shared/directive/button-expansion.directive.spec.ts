import { ButtonExpansionDirective } from './button-expansion.directive';

describe('ButtonExpansionDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonExpansionDirective();
    expect(directive).toBeTruthy();
  });
});
