import { ConvertINRPipe } from './convert-inr.pipe';

describe('ConvertINRPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertINRPipe();
    expect(pipe).toBeTruthy();
  });
});
